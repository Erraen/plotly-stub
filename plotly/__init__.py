"""Lightweight plotly stub package.

Provides the minimal interfaces required by Dash 4.x so the real plotly
(and its heavy dependencies like narwhals) can be excluded from the build.
"""

__version__ = "5.999.0"
